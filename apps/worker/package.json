{
  "name": "@widgetry/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Widgetry BullMQ worker (polling, snapshots, retention purge). STUB.",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "echo '[worker] dev not wired up yet - replace with tsx watch in Sprint 2' && exit 0",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo '[worker] no tests yet' && exit 0",
    "test:unit": "echo '[worker] no unit tests yet' && exit 0",
    "test:integration": "echo '[worker] no integration tests yet' && exit 0"
  },
  "devDependencies": {
    "@types/node": "^20"
  }
}
