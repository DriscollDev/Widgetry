// Placeholder. Sprint 2 replaces this with the BullMQ worker entry,
// the master scheduler tick (Engineering Doc §8.1), and the per-widget
// poll-job handlers in `src/fetchers/`.
export const placeholder = 'worker';
