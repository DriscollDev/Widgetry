// Placeholder. Sprint 1 adds a Zod-validated env loader so missing or
// malformed env vars (MASTER_ENCRYPTION_KEY, DATABASE_URL, etc.) fail loud
// at process start instead of mid-request. Env surface is documented in
// .env.example and Engineering Doc §16.2.
export const placeholder = 'config';
